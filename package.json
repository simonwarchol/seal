{
  "scripts": {
    "dev": "npm run build -- --sourcemap=inline --watch",
    "rspack": "rspack build --watch",
    "rsdev": "rsbuild dev --open",
    "build": "bun build src/seal/seal.jsx --format=esm --outdir=src/seal/static --asset-naming=[name].[ext]"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@anywidget/react": "^0.1.0",
    "@rsbuild/core": "^1.1.13",
    "@rsbuild/plugin-react": "^1.1.0",
    "@rspack/cli": "^1.1.8",
    "@rspack/core": "^1.1.8",
    "css-loader": "^7.1.2"
  }
}